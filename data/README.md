# Local application data

`scheduler.db` is created automatically on first launch and seeded with fictional demonstration posts. The SQLite database is ignored by Git; use **Reset Demo Data** in the app to recreate the sample schedule.
